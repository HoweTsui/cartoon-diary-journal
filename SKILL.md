---
name: cartoon-diary-journal
description: 小屁孩日记风格 Skill：创建原创、低噪点、人物一致的黑白幽默日记插画，并维护可拖拽缩放的 HTML 人物关系图库。用于把每日经历、照片摘要、聊天记录或事件清单整理为带日期和周几标题的竖版日记页，生成或更新固定人物形象、关系图谱、分镜提示词和参考资产，以及修复写实漂移、噪点过多、人物换脸或页面风格不统一等问题。
---

# 小屁孩日记风格 Skill（原创黑白幽默日记插画）

## 核心结果

把一天的经历压缩成一页原创 9:16 竖版日记：顶部固定日期与周几，下面让 3-6 个黑白墨线场景沿页面由上至下连续叙事，不画分格边框。统一使用“辨识度头型、双点眼、独立单鼻、双线长肢体、偏大扁鞋、纯黑块面”的原创骨感日记漫画结构，保持人物身份、干巴巴幽默和近乎零纹理一致。人物采用轻微侧向视角：前外侧眼旁的头部轮廓从发际高度到鼻根保留长断口，不画眉毛，也不闭合断口；鼻型的长短与形状从人物本身特征转绘，圆鼻或小鼻在自身轮廓上保留细小开口，不能画成完整闭合圆。所有后续用户上传图片都必须先经过同一套极简转绘协议，不能直接变成写实或另一套卡通风格。把长期人物维护在不含日期的可拖拽 HTML 全局关系图中。

不得复刻任何已出版作品的具体角色、标志性字体、页面、笑点或构图。用户点名某部作品时，只抽取“黑白、日记纸、稚拙简笔、夸张动作、短手写旁白”等非专属特征，再生成原创人物和场景。

## 按需读取

- 开始任何生图任务前读取 `references/style-system.md`；若任务包含宠物或明显情绪变化，同时读取宠物比例与表情规则。
- 出现人物或宠物时读取 `references/character-library.md`，先检索任务专用 HTML 图谱，再使用该图谱对应的人物图集。
- 选择页面结构时读取 `references/page-layouts.md`。
- 手工组提示词时读取 `references/prompt-template.md`；优先运行脚本自动组装。
- 生成后读取 `references/qa-checklist.md`。
- 使用用户照片或私密截图时读取 `references/privacy-originality.md`。
- 创建或更新关系图时读取 `references/character-library.md`，使用 `scripts/build_character_graph.py` 整体替换模板数据。

## 工作流

### 1. 提炼日记事实

先提取日期、标题、3-6 个事件、出场人物、每格一句短旁白和收尾句。缺少日期时使用用户明确指定的日期；不要从图片中的历史日期擅自推断今天。

每格只保留一个动作或笑点。优先记录小失误、反应、意外反差、生活细节和情绪变化，不把整天做成流水账。

### 2. 强制检索人物图谱

在写提示词或调用生图工具前，先读取任务专用 `character-graph.html` 的 `graph-data` 数据块，并逐一匹配本次日记的所有人物或宠物。任务图谱必须由本次实际数据整体构建，不能直接把仓库里的本地图谱当成默认人物库：

1. 依次用角色 ID、准确姓名、身份说明、外观锚点和人物备注匹配。
2. 全部命中时，直接使用对应图集格位和完整外观锚点；不得只凭姓名重新设计。
3. 任一角色未命中或匹配到多人时，立即暂停日记生图，明确列出缺失或歧义角色，并请用户为每个角色补充一张清晰图片。不要猜测、杜撰或用模板人物顶替；未完成补充前不得输出“已保持一致”的海报。
4. 用户补图后，只把原图作为当次临时身份输入；将角色转绘成与现有人物图集一致的原创黑白日记漫画，保留其鼻型、脸型、五官倾向、头身比与默认表情等身份特征，写入新图集格位，补齐 ID、姓名、身份、至少四个外观锚点、人物备注和实际关系，再重新构建任务 HTML，重新执行匹配检查，全部通过后才继续日记生成。不得把用户原图嵌入 HTML 或图集。

每次补图都必须执行“统一转绘协议”：

- 风格参考优先级固定为当前任务的 `character-lineup.png`，其次为 `assets/style-reference/diary-style-anchor.png`；用户上传图只能提供身份线索，绝不能覆盖线条、五官语法、肢体或头身比。
- 先提取发型轮廓、服装块面、配饰、体态、宠物耳尾和毛色等少量身份锚点，再转成极简黑色墨线角色；不保留皮肤纹理、真实毛发、摄影光影、材质褶皱或照片背景。
- 转绘结果必须复用图集的较粗纯黑线（以 1024px 宽画布为例约 5–6px，比旧版加粗 1–2px）、简化头型、双完整眼、单鼻、低位嘴、前外侧眼旁至鼻根的长轮廓断口、双线管状四肢、偏大扁鞋和克制纯黑块面；同一角色的轮廓、五官、衣物与道具线重必须一致，尤其两只眼的黑点尺寸与视觉粗细一致；人和动物的结构规则都不能因上传来源不同而改变。
- 输出先通过“风格门禁”：线重、五官位置、鼻型开口、四肢粗细、头身比、黑白分配与现有图集逐项一致；任一项不一致就重生成，不得用文字解释偏差。
- 动物按同一套极简线稿语法转绘，但身体比例必须保留各自物种与身份锚点：清晰的头、躯干、前后肢/脚掌、耳和尾，以及单一黑色动物鼻；鼻型与位置必须符合物种：狗鼻是口鼻最前端的单个实心黑椭圆，猫鼻应更小并放在两眼下方的面部中央。狗头必须明显大于躯干，双黑点眼间距偏宽；脚掌以短粗圆弧爪脚表现，每只脚只用 1–3 个简化趾口，不画解剖关节或细趾。动物眼睛只使用干净实心黑点，禁止白色高光、反光点或瞳孔层次。任何动物都不得套用人物的 C 形鼻、鼻梁或鼻孔。可参考动物风格图的身体比例，但不得复制其具体动物或细节。
- 表情只改变眼睛开合/圆睁、嘴洞/嘴角/露齿和不超过两个外置情绪符号；可用汗滴、泪滴、星星、爱心、光环、`Z`、小花或速度线表达状态。中性脸不画眉毛；愤怒或疑惑仅可在眼上方临时加 1–2 条短情绪线，不能改成常驻眉毛。禁止因表情改变头型、鼻子位置、发型、耳朵、四肢长度或鞋/脚掌结构，且双眼、单鼻、嘴始终完整。表情参考图只定义表情语法，不改变人物身份。

### 3. 用实际数据构建图谱

仓库中的 `assets/character-library/character-graph.html`、`character-lineup.png` 和任何示例人物都只是结构/风格参考，不是任何任务的默认人物库；公开发布时也不得把私人任务图谱或真人衍生头像作为默认数据。每次开始一个实际任务时：

1. 按 `assets/templates/character-graph-data.example.json` 创建任务数据文件。
2. 把 `nodes` 中每个人的 ID、姓名、身份、图集格位、位置、外观锚点和备注全部填写为本次实际数据。
3. 把 `edges` 的起点、终点、标签、类型和曲率全部填写为本次实际关系。不得追加在旧边之后。
4. 生成与实际人物一一对应的新 `character-lineup.png`，并放在任务 HTML 同目录。
5. 整体构建任务专用 HTML：

```bash
python3 scripts/build_character_graph.py <actual-character-data.json> \
  --output <task-output>/character-graph.html
```

构建脚本会把 `graph-data` 整块替换并标记为 `status: actual`。交付前把输出 HTML 的 `graph-data` 与任务数据逐字段比对；只要仍有任何未属于本次任务的旧姓名、旧身份、旧锚点、旧备注、旧关系端点或旧关系描述，就判定失败并重新构建。图谱标题只写“人物关系图”，不得加入日期、周几或时间线信息。

### 4. 生成稳定提示词

先填写 `assets/templates/diary-brief.example.json` 的同结构副本；`events.characters` 只写任务图谱中的实际角色 ID 或准确姓名，再运行：

```bash
python3 scripts/build_diary_prompt.py <brief.json> \
  --graph <task-output>/character-graph.html
```

脚本先确认图谱已是实际数据，再校验每个出场角色是否存在；缺失时会中止并要求补图。校验通过后自动计算中文周几并输出完整提示词。把输出中的日期标题原样传给生图工具，不要手工改写周几。

### 5. 锁定人物

把 `assets/style-reference/diary-style-anchor.png` 标为“风格参考”，把本次任务实际生成的人物图集标为“身份参考”。每次重复写出人物的头型、鼻型、眼嘴特征、发型、配饰、服装结构、头身比和固定表情；只写姓名不足以保持一致。

新人物先定义 4-6 个可观察锚点，再生成新版原创人物图集。不要把用户真人脸直接收入 Skill 资产；除非用户明确要求且确认允许长期保存。

### 6. 单页生成

使用内置 `image_gen`，每页单独生成。默认要求：

- 严格 9:16 长竖版；默认沿用参考母版的由上至下单页节奏，不画漫画分框。
- 顶部第一信息必须是 `YYYY.MM.DD 周X`。
- 日期下方放一句日记标题。
- 只用纯黑与纸白；纸面固定使用 14-18 条极浅青蓝横线，等距贯穿页面，不用其他颜色或蓝色装饰。
- 人物使用身份图集的头型、两个略拉开距离且同尺寸同粗细的黑点眼、侧后小耳、鼻下偏低的小嘴、稀疏发丝、细管躯干、双线管状四肢、细长小腿和偏大扁鞋；前外侧眼旁的轮廓从发际高度到鼻根保持长断口，不画眉毛。鼻型、嘴形与默认表情匹配身份图集。每人只有一个鼻子，鼻尖朝向与面朝方向一致；圆鼻、小鼻在鼻子自身轮廓保留开口，不能画成闭合 O；禁止方盒鼻、硬直角头、单眼侧脸、粗壮腿和发丝腿。人物脸部不得缺眼、鼻或嘴；动物必须保留物种需要的双耳、双眼、口鼻、四肢与尾巴，并使用无高光的实心黑点眼，不画真实毛发或写实眼睛。宠物仅保留清晰头、躯干、口鼻、耳、四肢、尾巴和不超过 3 块平面毛色；长毛品种最多使用 3–5 个大而圆钝的外轮廓缺口提示毛量，禁止逐撮毛刺、围脖毛束、毛流或蓬松渲染。
- 嘴、头发、上衣或鞋可用少量纯黑实填；禁止灰阶、渐变、柔和阴影和彩色点缀。
- 光滑纯白纸与极浅青蓝横线；禁止可见纸纤维、颗粒、噪点和复古做旧。
- 3-6 个由上至下的开放场景；每个场景一个动作；不堆背景、不画分格边框。对话气泡只在需要互动时使用，总数不超过 4 个，每个不超过 8 个字。

需要连续多天时，每天独立生成，但复用完全相同的风格参考、人物图集、画幅、线条和色彩限制。

### 7. 检查并迭代

按 `references/qa-checklist.md` 检查。出现写实光影、单眼侧脸、闭合圆鼻、鼻向与面向冲突、统一长鼻套脸、方盒鼻、直角头、五官缺失、粗壮腿、发丝腿、正常人体比例、写实宠物毛发或眼睛、灰色渐变、人物换脸、日期缺失、分格边框或可见噪点时直接判失败。一次只修一个问题；身份漂移时重新附人物图集，不靠文字补救。

### 8. 维护人物关系图

交付任务专用 `character-graph.html` 作为离线全局关系图库，不在图谱中加入日期。图谱必须支持节点自由拖拽、空白处平移、滚轮缩放、人物搜索和关系筛选；拖动任一人物靠近其他人物时，以平滑避让动画推开其他节点，并把人物姓名纳入碰撞范围，禁止节点或姓名互相遮挡。默认浏览态只显示纯文本。右侧按钮默认文案为“编辑”，点击后才显示人物编辑表单、新增人物和新增关系选择框；编辑态可双击连线名称进入内联编辑，按 Enter 保存，按 Escape 取消。左侧“编排”必须自动选择无环图的层次布局或有环/稠密图的力导向布局，使用 300-500ms 过渡动画并提供撤销、重做；节点超过 50 个时分帧计算，避免阻塞主线程。编辑态允许从节点的 `+` 控制点拖到另一节点自动吸附建连，也允许把已有连线端点向节点外拖出以断开关系。浏览器端修改使用按数据集隔离的 `localStorage` 自动持久化。

浏览器里的临时增删与编辑会保存在本机。需要把改动正式固化进 Skill 时同步更新三处：

1. `references/character-library.md` 的锚点与关系。
2. 任务 HTML 的 `graph-data` 数据块。
3. 任务人物图集的对应格位。

### 9. 发布前预检

发布或升级前运行 `python3 scripts/preflight_check.py` 检查入口文件、参考文档、脚本语法和链接；生成具体日记前必须额外传入本次任务的 `--graph` 与 `--brief`，让脚本验证角色命中、关系端点和日期周几。版本号写入 `VERSION`，变更记录写入 `CHANGELOG.md`，GitHub 展示说明见同目录 `README.md`。

## 隐私与参考资产

把用户提供的私人图片仅作为当次临时输入。只提取抽象特征，不复制姓名、日期、地点、品牌、脸、宠物花纹、商品、文案或构图。Skill 内只能保存重新生成的虚构参考资产，不能保存、复制、裁切或嵌入用户原图。

## 交付

把项目使用的最终图片和 HTML 放进当前 workspace。不要覆盖已有资产，除非用户明确要求替换。交付时报告：页数、日期、出场人物、保存路径、HTML 图谱路径以及仍需人工核对的文字。
