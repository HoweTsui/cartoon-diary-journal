---
name: cartoon-diary-journal
description: 小屁孩日记风格 Skill：把日常事实、照片摘要或事件清单整理为原创黑白幽默日记插画、固定人物图谱和离线可翻页日记本。强调 3:4 页面、半侧面人物、极细等粗肢体、受控表情、右侧备注栏和本地隐私边界。
---

# 小屁孩日记风格 Skill（原创黑白幽默日记插画）

## 核心结果

把一天的事实压缩成一张原创 3:4 竖版日记海报。日期和周几位于顶部并水平居中，标题在日期下方并水平居中；日期较小，标题约为日期的 1.25–1.35 倍，跨页面使用固定字号。页面使用白纸、清晰可辨识但不抢主体的浅青横线、同一档黑色墨线和固定的右侧备注栏。横线颜色、透明度、间距和粗细整页固定，不能接近白色而难以辨认。默认安排 4 个主叙事区；只有一件小事时放大为一幅主场景并保留 1–3 个真实相关细节，不拆成多页，不用漫画分格，也不删除事实。

人物采用原创骨感黑白日记漫画结构：辨识度头型、两枚正圆豆豆眼、人兽分开的单鼻语法、窄身板、极细双线肢体、偏大扁鞋和少量纯黑块面。每个可见人物或动物始终只能是 25°–35° 的偏正半侧面；禁止正面、近正面、标准 90° 正侧面（直侧脸）和单眼全侧面。鼻尖、脸颊、躯干、耳朵和肩线必须呈现方向性，不能对称成证件照。

人物头部必须先复用确认版图集的头宽/头高比例和身份轮廓，再处理动作、表情和身体；头部接近圆形或软圆角身份轮廓，头高至少约占站立全身高度的 30%，不能拉成长脸、成人脸或被夸张发型遮住。两眼位于头部上半区、略高、大小和基础圆直径一致，外侧眼不贴头部轮廓。人脸只保留双眼、一个清晰可见且独立的短鼻子和低位嘴；鼻子不能被嘴、脸部轮廓、手或道具遮掉，也不能省略或并入轮廓。发际/刘海末端到外侧眼上方只画短弧，短弧后到鼻根必须留下约 1.5 个眼点高度的清晰空白断口，断口内不许补眉线、额头线、鼻梁线或其他轮廓线。人物鼻子是短、横向、上方开口、不填黑的线描半椭圆，不能下垂成 U/C、不能闭合、不能有鼻孔点；动物只使用符合物种的单一实心黑鼻，不能套用人物鼻子。耳朵只保留干净的外轮廓，耳内留白，不画内耳弧线、耳窝线、阴影线或装饰纹。该头脸锁优先于场景、照片、表情、发型夸张和身体比例提示。

	自动化门禁关键词与上一版契约保持一致：人物必须是 **25°-35° 的偏正半侧面**，外侧眼不贴轮廓，且人头小于全身高度约 30% 时判定失败；人物鼻子必须清晰可见且绝不能实心填黑，动物使用符合物种的单一实心黑鼻。兼容短语“人物鼻子绝不能实心填黑”仍是硬门禁。每个场景都要为每一名可见人物或宠物单独写出角色 ID；每场必须有 4-10 字短总结，至少留出 1 条完整横线高度的场景间空白。日期和标题均水平居中，使用固定字号和右侧备注栏。眼点直径锁定为确认版图集的固定 canonical diameter；上臂/前臂管径不超过头宽约 1/22，当前首轮目标升级为全肢体同粗的约 1/32。头部、手掌/爪掌和鞋/脚的确认版尺寸保持不变；躯干、肩髋间距以及头部以下的四肢杆段长度统一按上一版角色卡的约 90% 处理，即身体比例缩短约 10%，不是拉长头部或压缩手脚。统一极细肢体与反正面锁（首轮即生效）要求首轮目标约 1/32；旧版首轮目标约 1/28 仅作为兼容上限，外轮廓宽度不超过该角色头宽的约 1/24（这是绝对上限）；肘、膝等转折使用轻微连续曲线，不画僵硬直角；袖口、裤脚等服装与肢体交界要有简洁清晰的分割线，不能混成一体。禁止正面、真正 90° 正侧面、正对镜头、对称双耳/双肩、鼻子居中。耳朵内侧保持空白无修饰线，人物、可见配饰（包括眼镜框、镜桥和镜腿）与背景环境的黑色线条不得局部忽粗忽细，配饰也不得单独使用发丝线；背景横线要清晰可辨识但保持低饱和浅青。需要时读取 `references/visual-atoms.md`；旧数据兜底只允许作为兼容入口，不能覆盖新规则。

## 风格触发词

兼容门禁短语“禁止正对镜头、对称双耳/双肩、鼻子居中”继续有效，并与当前禁止正面及真正 90° 正侧面的细化规则同时执行。

用户说“`小屁孩日记风格`”或“`Diary of a Wimpy Kid 风格`”时，启用原创 **骨感黑白日记锁**，只借鉴黑白、日记纸、稚拙简笔、夸张动作和短手写旁白等非专属特征，不复刻出版角色、Logo、签名字体、页面或笑点。触发后必须先运行 `scripts/build_diary_prompt.py`，不能凭记忆写一个泛化提示词。

触发词自动展开为：先复制确认版近圆头脸的宽高比例与五官位置，再处理场景；25°–35° 偏正半侧面；禁止正面、真正 90° 正侧面和单眼全侧面；两只等大的几何正圆眼点略高且外侧眼留白；眼下是短、横向、上方开口的不填黑人鼻，鼻下是分离的低位小嘴；短外轮廓弧后约 1.5 个眼点高度的空白断口；每个人物必须保留清晰可见的独立人类开放半椭圆鼻，动物使用单一实心物种鼻；耳朵内部留白、无修饰线；窄身板；上臂、前臂、大腿、小腿及动物不含爪掌的前后肢使用同一目标粗细约 1/32 的超细双线管，肘膝转折只用轻微曲线；旧版约 1/28 仅作兼容上限；头部、手掌/爪掌和鞋/脚保持确认版尺寸，躯干与头下身体段按 90% 比例（缩短约 10%）；短裤/长裤裤脚与小腿之间有白色分割，袖口和裤脚不能与肢体轮廓融合；统一黑色线宽且无局部粗细跳变；3:4 横线纸，横线使用清晰可辨识但克制的浅青色、固定透明度和间距；顶部居中日期标题；每行右侧备注；按事件驱动表情。头脸优先于照片、场景、表情和身体比例，禁止拉长头、放大眼睛、下垂长鼻、鼻嘴合并或让夸张发型遮眼。

**统一极细肢体与反正面锁（首轮即生效）**：人物上臂、前臂、大腿、小腿，以及动物不含爪掌的前后肢杆部，首轮目标升级为头宽约 1/32 的同一超细管径，彼此粗细误差不超过约 10%；旧版首轮目标约 1/28 只作兼容上限，`1/24` 只允许作为绝对失败上限，`1/22` 更是失败上限，不是绘制目标。头部、手掌/爪掌和鞋/脚尺寸保持确认版 canonical 尺寸；躯干、肩髋间距以及头部以下身体段按上一版角色卡的 90% 处理，身体比例缩短约 10%，不得拉长头部、压缩手脚或恢复成人比例。手掌/爪掌只在末端略大；裤料可以稍宽，但裤脚必须收窄到同样超细的小腿。任何手臂比腿粗、动物腿比人物腿粗、动作导致局部膨胀、身体被拉长、出现正面或真正 90° 正侧面、背景环境出现不同档线宽，都判为失败并整页重生。人物必须有窄躯干、偏大扁鞋、清晰裤腰/裆部分隔/裤脚线。

**眼睛和表情锁**：中性身份使用两枚同直径实心正圆点眼，直径锁定为确认版图集的固定 canonical diameter；表情可以从参考表情图选用有限原子 `neutral_dot`、`closed_arc`、`half_lid`、`wide_round`、`crossed`，但不能任意变形成椭圆、杏仁眼或写实眼。眼形原子只改变表情语义，不改变头型、眼位、鼻子、断口、身份轮廓或肢体；优先通过嘴洞/露齿、姿势、手势和不超过两个头外情绪符号表达强度。中性脸不画常驻眉毛。手部和脚部尺寸保持确认版；身体比例缩短约 10%。

## 参考图输入顺序

每次生图都要按 `references/visual-gate.md` 先验收，并把四张图一同作为 `image_gen` 的图像输入：`1) 本次实际 character-lineup.png`（只锁身份），`2) assets/style-reference/character-lineup-demo.png`（只锁确认版头脸、鼻型、肢体和裤脚结构），`3) assets/style-reference/face-geometry-closeup.png`（只锁眼睛原子、断口和人兽鼻型），`4) assets/style-reference/diary-layout-only.png`（只锁横线纸、日期标题、留白和纵向节奏）。若事件提供 `sourceImages`，再按场景逐张附在四张固定参考之后，只锁该场的可见事实，不锁画风；这些路径必须是仓库根目录下已存在的相对本地文件，禁止绝对路径、`..` 路径和远程 URL。少任何固定参考都停止生图。用户照片只能提供发型轮廓、服装块面、配饰、体态、动物耳尾等身份线索，不能提供摄影光影或写实解剖。

## 低门槛输入与事实提炼

日期或“今天”加一句话就足够。标题、4 个主叙事区、4–10 字备注、道具清单、表情和收尾句可以自动提炼；只有当身份、动作、数量或故事含义会改变时，才亲切地提出一个具体问题。高级 JSON 与轻量 `{ "date": "YYYY-MM-DD", "text": "..." }` 兼容；显式 `events`、`inventory`、`must_keep` 和 `flexible` 优先于自动提炼。

每个事件先做内部 scene inventory：`subjects`、`props`、`relations`、`must_keep` 和 `flexible`。如果有 `sourceImages`，逐图记录可见主体/动物数量、关键道具形状与数量、动作、表情、前中后景和相对位置；人物/动物、数量、核心动作、方向关系和关键道具进入 `must_keep`；次要道具和精确摆位进入 `flexible`，只允许在左/右/前/后/近/远等语义区域内微调，不得凭空添加改变含义的显著元素。表情必须先从日记事件推断，再选择嘴形、姿势和受控眼形原子。用户无需看到完整清单，只有关键歧义才显示一句简短追问。

默认一张海报四个主区；超过四件小事仍保留在清单中并按语义合并到四区，不缩小文字、不制造网格、不删除事实。每个角色在一个主场景中默认只出现一次；只有用户明确要求连续动作、镜像、视频或同时承担不同动作时才重复，并继续使用同一角色卡。只有一件小事时用一幅更大的 hero scene，加 1–3 个真实相关细节和干净留白，禁止发明事件或重复填充人物。

## 人物图谱和转绘

开始生图前必须检索任务专用 `character-graph.html` 的 `graph-data`。每个场景为每一名可见人物或宠物单独写出角色 ID、图集 `col`/`row`、`avatarSrc` 和全部锚点；主角不能成为配角或宠物的默认模板。全部角色命中后才可继续；缺失或歧义时暂停并请用户补一张清晰图，不猜姓名、品牌、文字或数量。

新角色先提取 4–6 个可观察身份锚点，再统一转绘为 1:1 方形上半身头像，头像完整居中，写入节点唯一 `avatarSrc`，并将头像按同一顺序拼入总图集。统一转绘优先级是结构硬门禁 > 当前任务身份 > 版式：头型、半侧面、眼睛、鼻子、断口、细肢体和线宽不能因照片或配角身份而改变。旧图谱没有独立头像时，进入日记本前必须完成头像迁移。

## 单页提示词与 ImageGen

运行：

```bash
python3 scripts/build_diary_prompt.py <brief.json> --graph <task-output>/character-graph.html
```

生成器输出完整的上一版连续提示结构：必需图像输入、PRIMARY DRAWING GATE、P0 geometry lock、P0 visual gate、逐场景角色卡、story scene inventory、canonical identity anchors、relationships、style lock、composition、identity lock、upload conversion lock、originality/privacy 和 avoid。每个场景必须写一个主动作、最多一个次动作、真实道具关系、事件驱动表情、4–10 字右侧备注；标题不超过 12 字、气泡不超过 6 字。ImageGen 负责按固定层级渲染文字；出现错字、缺字、乱码、额外文字或标题未居中时整页重生成。

页面只用纯黑、纸白和清晰可辨识的低饱和浅青横线；人物、动物、道具、家具、建筑、可见配饰（包括眼镜框、镜桥和镜腿）和背景环境使用同一档稳定黑墨线，禁止局部忽粗忽细或出现发丝线。横线保持统一颜色、透明度、间距和线宽，不能淡到接近白色。3:4 是新页面默认比例；历史 9:16 海报作为兼容输入完整 `contain` 放入 3:4 书页，禁止裁切、拉伸或压扁。

## P0 门禁和修复

出图前逐项确认 `must_keep`、数量、空间关系、角色卡和参考图职责。出图后逐角色检查：只能 25°–35° 偏正半侧面，不能出现正面或真正 90° 正侧面；头部比例、双眼圆直径、外侧眼留白、清晰可见且独立的鼻子、空白断口、耳内空白、约 1/32 且等粗的超细四肢、头部/手部/脚部确认版尺寸、身体比例缩短约 10%、裤脚分割、动物物种鼻子和尾耳均要保持。背景环境和可见配饰（包括眼镜框、镜桥和镜腿）出现不同档线宽、鼻子丢失、粗手臂/腿、人物下垂鼻、动物人类鼻、耳内修饰线、眼睛任意变形或右侧备注缺失，都是 P0 失败。

非关键道具或摆位漂移最多自动修复两轮；身份、几何、关键事实、文字或比例失败必须整页重生成。两轮仍不清楚时只问一个亲切、具体的问题，不让用户重复填写整份表单。验收清单见 `references/visual-gate.md` 和 `references/qa-checklist.md`。

## 人物关系图与离线日记本

使用 `scripts/build_character_graph.py` 整体构建无日期的任务图谱，支持节点拖拽、缩放、搜索、关系筛选和 `character-graph.html?character=<角色ID>` 自动选中居中。关系图内的人物介绍使用独立 `1:1 方形上半身头像`，不从总图集裁切上半身。

用户要求翻页、时期、索引或人物联动时读取 `references/diary-book-system.md` 并运行 `scripts/build_diary_book.py`。离线日记本保留日期、目录、搜索、`#entry=<日记ID>` 深链、阅读进度和人物跳转；每张书页为 3:4，历史 9:16 海报完整 contain。真实海报、头像、图谱和 `task-output/diary-book/` 只留本地并被 Git 忽略，公开 Demo 仅使用虚构数据。

## 维护、隐私与交付

发布前运行 `python3 scripts/preflight_check.py`、Python/JavaScript 语法检查和 `git diff --check`。公开仓库只提交模板、虚构 Demo、固定本地运行时、参考资产和说明文档；不使用 CDN、远程字体、远程图片、分析脚本或用户原图。版本号写入 `VERSION`，变更写入 `CHANGELOG.md`。交付时说明实际海报/HTML 路径、出场人物、页面比例、测试结果和仍需人工核对的文字；不要把未通过 P0 门禁的图当作“接近完成”。
