# 离线翻页运行时

本目录的 `vendor/page-flip.browser.js` 固定来自 `page-flip@2.0.7`（StPageFlip）。

- 上游：https://github.com/Nodlik/StPageFlip
- 许可：MIT，完整文本见 `vendor/STPAGEFLIP-LICENSE`。
- 不使用 CDN；构建日记本时会把运行时复制到输出目录的 `runtime/`。

日记页必须是静态 HTML。人物关系图保留为单独的 HTML 页面，不嵌入翻页页内。
